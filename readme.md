## Imputations

This repository contains a small package to impute missing data for the estimation of inequality statistics such as the Gini index.

## Installation

You can install the development version from GitHub:

```r
# install.packages("remotes")
remotes::install_github("hip-nl/imputations", ref = "package")
```
